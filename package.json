{
  "name": "pattiserie-fine",
  "version": "1.0.0",
  "description": "Gateau au desserts fait a la main",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-sass": "node-sass --include-path scss scss/index.scss css/index.css",
    "watch-sass": "nodemon -e scss -x \"npm run build-sass\" "
  },
  "author": "Carlos Raez",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^6.0.1",
    "nodemon": "^2.0.12"
  }
}
